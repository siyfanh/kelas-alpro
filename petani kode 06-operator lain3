#include <stdio.h>

void main()
{
    int z = 10;

    // increment a
    z++;

    printf("z++ = %d\n", z);

    // increment z
    ++z;

    printf("++z = %d\n", z);

    // decrement z
    z--;

    printf("z-- = %d\n", z);

    // decrement z
    --z;

    printf("--z = %d\n", z);
}
